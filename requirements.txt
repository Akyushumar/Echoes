# Core dependencies
google-genai>=1.0.0         # Gemini API for emotion analysis
python-dotenv>=1.0.0        # Environment variable loading
typer[all]>=0.9.0           # CLI framework
pydub>=0.25.0               # Audio format conversion
rich>=13.0.0                # Pretty terminal output

# STT providers
openai>=1.0.0               # OpenAI Whisper API fallback
openai-whisper              # Local Whisper STT (last-resort fallback)
sarvamai>=0.1.0             # Sarvam AI STT + Chat (primary STT + emotion fallback)

# Phase 2 dependencies
sounddevice>=0.5.0          # Live mic recording (CLI only, not used on cloud)
scipy>=1.11.0               # WAV file writing for sounddevice

# Phase 3 dependencies
streamlit>=1.30.0           # Web UI
plotly>=5.18.0              # Interactive charts
